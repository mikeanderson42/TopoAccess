# How It Works

```text
workspace profile
-> repo/cache/release paths
-> TopoGraph and deterministic exact tools
-> prompt pack / tool schema
-> agent adapter
-> provenance-checked response
```

Exact lookup remains tool-only. Preferred model fallback remains category-gated.
