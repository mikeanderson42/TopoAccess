# Harness Compatibility

V37 compatibility targets:

- Codex: `AGENTS.md`, Codex brief, post-edit validation.
- Claude Code: hook settings example and tool schema.
- Cursor: `.mdc` rule file.
- Aider: token-budgeted repo map.
- Continue/OpenHands: HTTP/OpenAPI schema.
- OpenClaw/Hermes/generic: tool schema and prompt packs.
- HTTP/stdio: local tool transports.

If a client is not installed, TopoAccess reports docs/simulated support rather than claiming real-client validation.

