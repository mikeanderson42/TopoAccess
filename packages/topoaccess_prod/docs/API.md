# API

CLI commands return a human-readable status line followed by JSON.

Important fields:

- `preferred_model_verified`
- `nonpreferred_model_used`
- `exact_lookup_tool_only`
- `category_gated_model`
- `metrics`
- `route`
- `trace_id`
