# Change-Planning Optimization

V35 improves the weakest V34 token-savings category by using:

- deterministic plan skeleton,
- smaller change-plan prompt pack,
- reusable risk/test/command templates,
- planning-pattern cache,
- stricter file/test extraction from TopoGraph.

If change-planning does not improve in a future benchmark, report the category as already near ceiling.

