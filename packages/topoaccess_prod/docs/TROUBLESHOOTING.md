# Troubleshooting

If native service install is unavailable, use the wrapper service path. If preferred model identity cannot be verified, disable model categories and preserve exact tool-only lookup.
