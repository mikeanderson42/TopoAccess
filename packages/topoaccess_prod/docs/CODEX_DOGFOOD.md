# Codex Dogfood

V36 uses read-only Codex fixture loops.

The smoke runs `codex --version`, generates adapter-backed Codex briefs, and uses fixture tasks for post-edit validation without destructive repo edits.

