# Real Codex Use

V35 runs a read-only Codex smoke:

```bash
codex --version
python packages/topoaccess_prod/scripts/topoaccess_agent.py codex-brief --profile default --task "Improve exact command lookup resolver"
```

No repo edits are made by the smoke. Do not send secrets to external tools.

