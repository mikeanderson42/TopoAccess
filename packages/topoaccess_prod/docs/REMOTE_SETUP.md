# Remote Setup

TopoAccess does not invent a remote URL or push automatically.

If no remote is configured, add one explicitly:

```bash
git remote add origin <URL>
git push -u origin topoaccess-prod-v37-harness-compat
```

Run safe publish dry-run and release scans before any push.

