# Release Archive

V38 creates a release archive containing the production package, docs, release manifests, install instructions, and compatibility artifacts.

The archive excludes model files, GGUFs, caches, runtime logs, secrets, and env files.

