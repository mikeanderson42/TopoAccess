# CI

V38 provides both local CI and a GitHub Actions template.

Local CI runs product tests, release validation smoke, artifact audit, secret scan, adapter smoke, and conformance checks.

```bash
packages/topoaccess_prod/ci/run_local_ci.sh
```

