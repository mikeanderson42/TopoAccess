# Why TopoAccess

TopoAccess exists because coding agents need precise repo intelligence, not raw-context overload.

Direct preferred-model use only wins or matches on unconstrained narrative phrasing. TopoAccess beats direct preferred-model use on exact lookup, provenance, safety, latency, and model invocation rate by routing exact tasks through deterministic tools and providing compact provenance-backed context packs.
